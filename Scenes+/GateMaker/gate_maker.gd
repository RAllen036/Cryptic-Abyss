class_name GateMaker
extends Node

# Look to have / use different dungeon making algorithms

enum WorldType {
	DESERT = 0,
	GRASSLANDS = 1,
	MOUNTAIN_range = 2,
	
	}

func generate(type: WorldType) -> BaseGate:
	return null
